# ─── AI (analyze + explain) ─────────────────────────
# primary (권장): Gemini 2.5 flash — 대용량 PDF 입력 + Vision OCR 지원
GEMINI_API_KEY=

# fallback: Anthropic Claude — Gemini 503 일시 장애 시 자동 전환
ANTHROPIC_API_KEY=

# (선택) AI_GATEWAY_API_KEY — ANTHROPIC_API_KEY 대체 가능

# ─── Vercel Blob (PDF 임시 저장) ───────────────────
# https://vercel.com/dashboard/stores
BLOB_READ_WRITE_TOKEN=
