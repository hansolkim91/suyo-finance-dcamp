# 진행 현황: 수요재무회
마지막 업데이트: 2026-04-21 (v3 1단계 완료, 사용자 PDF 검증 대기)

---

## v3 진행 상황 (Plan.md v3 기준)

### [x] 1단계: 신규 필드 추가 (2026-04-21)
**최종 구조: PDF 전체 텍스트 → AI 1회 호출 → 통합 스키마 결과 → UI 표시**

- `src/lib/finance/types.ts`: 통합 `yearDataSchema` 1개 (서브스키마 분리 안 함). 신규 필드 2개 추가:
  - `inventory` (재고자산, 당좌비율 계산용)
  - `restrictedCash` (사용제한 현금, 비상장 현금 3분류용)
- `src/lib/finance/extractLineItems.ts`: 정규식 폴백에도 inventory·restrictedCash 키워드 추가
- `src/lib/ai/gateway.ts`: PDF 전체 텍스트를 1회 generateObject 호출로 전달 (섹션 추출 로직 제거)
- 검증: `npx tsc --noEmit` 통과, dev 서버 HMR 정상

#### 시행착오 기록 (참고)
- 최초 시도: nullable 16개 한도 우회를 위해 3개 서브스키마 분리 + 3회 병렬 호출 + 카테고리별 텍스트 구간 전달
- 문제 발견: 사업보고서 PDF는 본문 시작 전에 목차에 "포괄손익계산서/재무상태표/현금흐름표" 단어가 모두 등장 → 마커가 목차 위치(2,500~3,300자 지점)에서 잡혀 본문 도달 못함 → 모든 필드 null
- 더 큰 문제: "PDF 전체 → API 1회 → 원하는 형태로 받기"라는 한솔 님의 큰 그림과 어긋남
- 해결: 분할 호출 전부 롤백 → 전체 텍스트 1회 호출로 통일. nullable 18개는 Gemini 2.5 flash가 무리 없이 처리

#### 남은 검증 (한솔 님 액션)
- 상장사 PDF 재업로드 → 모든 필드가 실제 값으로 채워지는지
- inventory·restrictedCash 추출 확인
- 응답 시간 ~15초 예상

### [x] 2단계: 상장 유동성 지표 3종 + 신호등 임계값 (2026-04-21)
- `src/lib/finance/metrics/listed.ts`:
  - `quickRatio()` 함수 추가 — 당좌비율 = (유동자산 - 재고자산) / 유동부채 × 100
  - `cashRatio()` 함수 추가 — 현금비율 = 현금 / 유동부채 × 100
  - `calculateListedMetrics()`에 당좌비율·현금비율 2개 metric 추가 (유동비율은 기존)
  - 총 14개 → 16개 지표
- `src/lib/finance/thresholds.ts` (신규):
  - 4카테고리 분류 객체 (`CATEGORY_METRICS`): 성장성 / 수익성 / 안정성 / 유동성
  - 신호등 임계값 상수 (`THRESHOLDS`): 17개 지표별 good/warning + invert 옵션
  - `getSignalStatus()` 헬퍼 — 값 → "good" | "neutral" | "warning"
- 설계 포인트: metrics/listed.ts의 기존 category("수익성/안정성/성장성/효율성") 그대로 두고, 화면 분류는 thresholds.ts에서만 관리 → 회귀 위험 ↓
- 검증: `npx tsc --noEmit` 통과

### [x] 3단계: 비상장 신규 지표 4종 (2026-04-21)
- `src/lib/finance/metrics/private.ts`에 4개 export 함수 추가:
  - `calcRunwayScenarios(latest)` → baseline / tight(-20%) / growth(+20%) / worst(매출 -30%) 4시나리오 + 가정 주석
  - `calcBepReverse(y)` → 흑자 전환 필요 매출·성장률. 공식: `고정비 / (1 - 변동비율)`. 이미 흑자면 null + isAlreadyProfitable 플래그
  - `calcCashPosition(y)` → immediate / restricted 분리. restrictedCash null이면 전액 즉시가용 + 가정 주석
  - `calcBurnYoY(latest, prior)` → Gross/Net Burn 월평균 전년 대비 %
- 설계: 기존 `calculatePrivateMetrics()` 그대로 두고 심화 지표는 별도 export 함수로 → UI에서 개별 호출 (7단계 화면에서 사용)
- 검증: `npx tsc --noEmit` 통과

### [x] 4단계: 네이버 금융 스크래핑 API (2026-04-21)
- 신규 파일 3개:
  - `src/lib/peers/types.ts` — `PeerData` 타입 정의
  - `src/lib/peers/naverFinance.ts` — 정규식 기반 HTML 스크래핑 + 1시간 메모리 캐시
  - `src/app/api/peers/route.ts` — POST 엔드포인트 (단일 stockCode 또는 stockCodes 배열, 최대 10개)
- 추출 항목 8개: 회사명, 시가총액, PER, PBR, EPS, BPS, 52주 최고/최저, 현재가
- cheerio 미사용 — 정규식 충분 + 의존성 추가 안 함
- 검증: `curl -X POST localhost:3000/api/peers -d '{"stockCode":"005930"}'` → 삼성전자 모든 필드 정상 추출 (PER 36.84, 시총 1,280조 등)

### [x] 5단계: AI 종목코드 + 동종업체 추천 (2026-04-21)
- `src/lib/finance/types.ts`:
  - `peerSuggestionSchema` 신규 — `{ name, code }`
  - `financialDataSchema`에 `stockCode: string | null`, `peerSuggestions: PeerSuggestion[]` 추가
- `src/lib/ai/gateway.ts` 프롬프트 보강:
  - `stockCode`: 6자리 종목코드 추출, 비상장사면 null
  - `peerSuggestions`: 동종업종 한국 상장사 3개 추천 (해외 회사 절대 제외, 시가총액 비슷, 본 회사 제외, 비상장사면 빈 배열)
- `src/lib/finance/extractLineItems.ts` (정규식 폴백)도 새 필드 채움 (stockCode=null, peerSuggestions=[])
- 검증: `npx tsc --noEmit` 통과
- **실제 PDF 검증은 7단계 화면 작업 후 또는 dev 서버 콘솔 로그로 가능**

### [x] 6단계: ResultView 파일 분리 (2026-04-21)
- ResultView.tsx 1378줄 → **53줄(분기만)** + 8개 파일로 분리
- 신규 파일 9개:
  - `shared/types.ts` (46) — ChecklistItem, ChecklistResult, Metric, YearMetrics, Status
  - `shared/format.ts` (34) — formatWon/Percent/Ratio, calcYoy
  - `shared/badges.tsx` (91) — statusDot, yoyBadge, getMetricStatus, getScoreColor 등
  - `shared/ScoreGauge.tsx` (59)
  - `shared/CategoryCard.tsx` (80) — CategoryCard, KpiMini
  - `shared/CategoryRadar.tsx` (60)
  - `ResultViewListed.tsx` (538) — 상장 모든 섹션 + 메인 (7단계에서 4섹션 재구성하며 더 축소 예정)
  - `ResultViewPrivate.tsx` (23) — 임시 스텁 (Listed 그대로 재사용, 8단계 재작성 예정)
  - `ResultView.tsx` (53) — type 분기 + ChecklistResult/ChecklistItem/CategoryScores re-export (AnalysisPanel 호환)
- 화면 동작 그대로 (회귀 없음), Next.js HMR 정상
- 검증: `npx tsc --noEmit` 통과

### [x] 7단계: 상장 화면 4섹션 재구성 + 밸류에이션 + 동종비교 (2026-04-21)
- `ResultViewListed.tsx` 완전 재구성 (538줄 → 672줄, 중복 제거 + 신규 카드 통합)
- 화면 구조 (위→아래):
  1. 회사명 + 종목코드 + 종합 점수
  2. AI 종합 의견 (whitespace-pre-line으로 줄바꿈 보존)
  3. 핵심 재무 요약표 (절대 금액, 1회만)
  4. 4섹션 카드 — 성장성 / 수익성 / 안정성 / 유동성 (각각 차트 + 표)
  5. 밸류에이션 카드 (네이버 금융)
  6. 동종업계 비교 표 (네이버 금융 다중)
  7. 카테고리별 레이더
- **중복 제거 완료**:
  - `FinancialRatiosTable` (11개 합본 표) 삭제 → 4섹션으로 분산
  - `ProfitFlowSection`의 KPI 카드 4개 삭제 → 마진 차트 + 표로 통합
  - `BusinessModelSection` (회사 정보 카드) 삭제 → 헤더로 흡수
  - `EvaluationSection` (ROE/ROA) 삭제 → 수익성 표로 흡수
  - `RiskSection` (리스크 체크리스트) 삭제 → AI 종합 의견에 포함
- **차트=숫자 원칙 적용**:
  - 매출/영업이익 콤보 차트 → 별도 매출 KPI 카드 X
  - 마진 추이 라인 차트 → 별도 마진 KPI 카드 X
  - 차트로 표현 어려운 ROE/ROA/이자보상배율 등은 표로만
- 신규 컴포넌트 2개:
  - `listed/ValuationCard.tsx` (144줄) — useEffect로 `/api/peers` 호출, PER/PBR/PSR/시가총액/52주
  - `listed/PeerComparisonTable.tsx` (185줄) — 본 회사 + 추천 3개 동종업체 한 표 비교
- `thresholds.ts`의 `CATEGORY_METRICS` + `getSignalStatus` 사용 — 4섹션 분류와 신호등 일관성
- 검증: `npx tsc --noEmit` 통과, Next.js HMR 정상

### [x] 7단계 보완 (2026-04-21)
추가 요청 3건 처리:
1. **단위 표시**: 1조 이상 "X.X조"로 일괄 표시 (`formatChartAmount` 헬퍼) — 차트 Y축·Tooltip 적용
2. **호버 도움말 UI/UX 개선**: HTML title → `InfoTooltip` 컴포넌트 (Tailwind group-hover, 즉시 표시, 예쁜 박스, ⓘ 아이콘)
   - `thresholds.ts`에 `METRIC_DESCRIPTIONS` 매핑 추가 (17개 지표 모두 명시 설명)
3. **네이버 PER/EPS 추출 실패 버그 수정**: SK하이닉스(음수 PER) 같이 `<em class="f_up">`으로 감싸진 값을 못 잡던 정규식 개선 → em 태그 선택적 매칭
   - 캐시 로직 개선: 전부 null인 응답은 캐시하지 않음 (정규식 수정 시 재조회 가능)
   - 검증: `curl localhost:3000/api/peers -d '{"stockCode":"000660"}'` → PER -11.30, EPS -12,517 정상
4. **현금창출력 점수 프롬프트 개선**: 상장사 metrics에 영업CF 없을 때도 영업이익률로 간접 판단하는 결정론적 기준 추가 (영업이익률 15%↑·부채비율 100%↓ → 90점 등). 0점·20점은 명백한 적자 때만.

### [ ] 8단계 ~ [ ] 9단계: 대기

---

## 이전 세션 (MVP 완료) — 보존용

### 직전 상태
- MVP 1~8단계 완료, Gemini 유료 Tier 1 전환, 로컬 스모크 통과
- **고도화 라운드(v2) Plan.md 작성 완료** → v3로 통합 재작성됨
- **아직 구현은 시작 전** — 다음 세션에 Plan.md 1단계부터 착수
- Vercel 배포(9단계)는 **고도화 라운드 끝난 뒤** 별도 진행

### 고도화 라운드 v2 개요 (Plan.md 참고)
1. 스키마 확장 + AI 추출 프롬프트 갱신 (Capex·투자CF·재무CF·영업외수익·사용제한현금 5개 필드 추가)
2. 상장 지표 엔진 확장 (FCF, DuPont 3분해, 순차입금, 지원금 의존도)
3. 비상장 지표 엔진 확장 (Runway 4시나리오, BEP 역산, 현금 포지션 3분류, Burn YoY)
4. 상장 UI 고도화 (EvaluationSection 6개 지표로 확장, thresholds 분리)
5. 비상장 UI 고도화 (시나리오 표, BEP 역산 카드, 현금 포지션 스택, Burn YoY)
6. AI 프롬프트 고도화 (상장/비상장 분기, 구체 임계값 루브릭, ACTION 제안 헤더 강제)
7. 로컬 스모크 + 현황.md 정리

### 레퍼런스 파일
- `C:\Users\디캠프\Desktop\랜식_재무분석_BurnRate.xlsx` — 스타트업 분석 사례 (비상장 설계 방향성 참고, 복제 대상 아님)

### 권한 설정 (2026-04-17)
- `.claude/settings.local.json` 대폭 확대: Read/Edit/Write/Glob/Grep/Agent/WebFetch/WebSearch + Bash(npm/npx/node/git 대부분/python/curl 등) 자동 허용
- deny 명시: `git push*`, `rm -rf /*`, `sudo *` 등 위험 명령만 차단
- defaultMode: `acceptEdits`

### 최근 변경 사항 (2026-04-17)

#### UI 대시보드 전면 개편
1. **핵심 재무 요약표**: 매출액/영업이익/순이익 등 절대 금액 연도별 표 + 전년 대비 증감
2. **주요 재무 비율 표**: 매출원가율, 판관비율, 영업이익률, 순이익률, 부채비율, ROE, ROA 등 11개 비율 + 상태 배지
3. **매출·이익 추이 차트**: 매출 막대 + 영업이익/순이익 라인 콤보 차트
4. **5개 카테고리 독립 카드**: 각각 KPI + 차트 + AI 해설
   - 사업모델: 주요 회사 현황 표 + AI 분석
   - 매출·이익 흐름: 수익성 KPI + 성장률 + 추이 라인 차트
   - 재무건전성: 안정성 KPI + 현금 현황 + 부채vs자본 스택 차트
   - 리스크 신호: 자동 판단 체크리스트 (자본잠식/부채급증/적자 등)
   - 비교·평가: ROE/ROA 효율성 KPI
5. **종합 점수 반원 게이지** + **카테고리별 레이더 차트**
6. **AI 종합 인사이트 박스**: 파란 보더 하이라이트

#### API 변경
- explain API: categoryScores, insight, overallScore 필드 추가
- 현금창출력 0점 → 데이터 없으면 50점 기본값
- 사업모델 AI 분석 6~8문장으로 강화

#### PDF 파싱 변경
- v1: 마커 1개 + 60,000자 → 일부 누락
- v2: 전체 전달 → API 토큰 한도 초과
- **v3 (현재)**: 3대 재무제표(손익계산서/재무상태표/현금흐름표) 구간 각각 추출 → 합쳐서 ~120,000자 전달

### 블로커 (해소됨 ✅)
- ~~**Gemini 무료 한도**: 분당 250,000 토큰 → PDF 전체(~370,000 토큰) 초과~~
- ~~**Anthropic 한도**: 분당 30,000 토큰 → 더 엄격~~
- **2026-04-17**: Gemini 유료 티어(Tier 1) 전환 → 분당 100만 토큰, PDF 전체 전달 가능
- **로컬 스모크 테스트 통과** (localhost:3000, PDF 업로드→파싱→지표→AI 해설 정상)

### 다음에 Claude에게 할 말 (복붙용)
> "수요재무회 프로젝트 이어서 하자. 현황.md 읽고 이어서 진행해줘."

### 환경변수 상태
- **BLOB_READ_WRITE_TOKEN**: .env.local 저장 완료
- **ANTHROPIC_API_KEY**: .env.local 저장 완료 (sk-ant-api03-HAK9pe...)
- **GEMINI_API_KEY**: .env.local 저장 완료

---

## 완료된 단계

### [x] 1단계: 프로젝트 초기화 + UI 기본 세팅
### [x] 2단계: 탭 뼈대 + 업로드 UI 레이아웃
### [x] 3단계: Vercel Blob 클라이언트 업로드
### [x] 4단계: PDF 파싱 (unpdf + LLM 폴백)
### [x] 5단계: 지표 계산 엔진 (상장 14개 + 비상장 4개)
### [x] 6단계: 결과 화면 → 재무 선배 스타일 대시보드
### [x] 7단계: AI 해설 → 카테고리별 카드 통합
### [x] 8단계: Blob 정리 + 에러 UX

---

## 남은 단계
- [ ] **고도화 라운드 v2** (Plan.md 7단계) — 다음 세션 착수
- [ ] 9단계: GitHub 푸시 + Vercel 배포 + 스모크 테스트 (고도화 라운드 완료 후)

## 커밋 이력 (이번 세션)
- `bf44802` — 체크리스트 UI 개편 + 전체 코드
- `b134ee0` — 현황.md 업데이트
- `c5ea663` — 재무 선배 스타일 대시보드 전면 개편
- `21f0647` — PDF 전체 텍스트 전달 변경
- `1fee45c` — 주요 재무 비율 표 추가
- `1065a8e` — 재무 비율 순서 변경 + 사업모델 개선 + 현금창출력 수정

## 발견한 이슈
- **Anthropic API 분당 30,000 토큰**: PDF 전체 전달 불가 → Gemini로 전환
- **Gemini 무료 분당 250,000 토큰**: PDF 전체(~370,000 토큰) 초과 → 3구간 추출 또는 유료 전환
- **pnpm 사용 불가**: npm으로 전환 확정
- **Blob Store Private 모드**: access: "private" 사용
